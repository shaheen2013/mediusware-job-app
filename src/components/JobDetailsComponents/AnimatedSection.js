import React, {useContext} from 'react';



const AnimatedHeader = ({name,navigation}) => {
    
    return (
        
    );
};
const styles = StyleSheet.create({
   
})

export default AnimatedHeader;
